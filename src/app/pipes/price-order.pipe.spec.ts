import { PriceOrderPipe } from './price-order.pipe';

describe('PriceOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
